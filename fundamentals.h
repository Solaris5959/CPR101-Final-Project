// FUNDAMENTALS MODULE HEADER
#ifndef _FUNDAMENTALS_H_	// if the fundamentals.h file has been run already, it will not run again (ifndef means "if not defined")
#define _FUNDAMENTALS_H_	// define _FUNDAMENTALS_H_ as to prevent reruns of the header file
#include <stdio.h>			// include standard I/O lib
#include <stdlib.h>			// include the standard library functions lib
#include <string.h>			// include the string manipulation lib
void fundamentals(void);	// declare prototype of function fundamentals
#endif						// end the conditional definitions